---
description: Show the office roster
---

# The office roster

Regenerate and display the roster by running:

```
python "${CLAUDE_PLUGIN_ROOT}/bin/render_roster.py"
```

Show the rendered cards to the user. If there is no `.claude-office` yet, offer
to set one up by running `python "${CLAUDE_PLUGIN_ROOT}/bin/install.py"`.
